# Agent Notes

- For Ruby/Bundler and Jekyll commands in this repository, run them with `BUNDLE_PATH=vendor/bundle`, for example:
  - `BUNDLE_PATH=vendor/bundle bundle install`
  - `BUNDLE_PATH=vendor/bundle bundle exec jekyll build`
